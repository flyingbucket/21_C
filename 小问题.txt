在采购原材料时，我需要给出一个选取供货商的方案；
每周采购一次，以周为单位给出供货方案，需要一次性给出未来24周的供货方案；
周之间的关系我希望用遗传算法来体现，这一点暂时不谈；
决策变量y_ij是0或1布尔值，表示第i周是否从第j家供货商进货；
供五十家供货商可以备选，依据历史表现为它们打了分，它们的信息储存在两个excel表格中；
目标函数有两个，一是让未来一周所需供货商总数量最小，二是让所选取的供货商总评分之和最大，实现多目标优化，两目标函数权重各位50%；
约束条件和具体数据暂时不谈，请你设计一个python程序实现目标函数和决策变量的构造。


# 目标函数2：最大化所选取的供货商总评分之和
def objective_function2(y):
    total_score = 0
    for j in range(num_suppliers):
        if y[j] == 1:
            total_score += supplier_scores[j]
    return total_score
对于目标函数二，我们不必使用繁琐的for循环和if判断；
由于决策变量y是布尔值，选择第j个供货商则y[j]=1,不选择第j个供货商则y[j]=0；
那么我们可以将决策变量与供货商得分相乘再相加，这样同样可以求得总得分。
请你作出修改，只要输出有关目标函数二的代码块即可，不要把整个代码写一遍。

suppliers_scores = pd.read_excel('scores.xlsx', usecols=[1, 2], header=0)
supplier_ids = suppliers_scores['B'].values
supplier_scores = suppliers_scores['C'].values
如果说第二列和第三列表头分别为B和C，那么上面的代码能正确读取数据吗


1.xlsx文件中，提供了过去240周供应商的供货数据，
第一列是供应商id，第二列是供应商提供的材料类别（这一点在本程序中没用），
第三列到第242列是供应商第一周到第240周的供货量，
第一行表头，
这一表格中共有402家供货商国王240周的数据，我只想分析其中50家的数据，
需要被分析的供货商的id都记录在scores.xlsx的第一列，scores.xlsx的第一行是表头，
这两个表格的供应商id有一点不同scores.xlsx只记录了数字，而1.xlsx在数字前还加上了S，在检索时需要注意；
请你利用ets法分析并预测未来24周各家企业的供货量，写出对应的python代码。

scores.xlsx中的Supplier ID与1.xlsx中的供货商ID都表示供货商的ID号码，
1.xlsx提供供货商的原始数据，scores.xlsx中的供货商是我感兴趣的研究对象。
请将我感兴趣的研究对象的原始数据提取出来形成新的excel表

